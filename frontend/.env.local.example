# Harmonix FastAPI — used by Next server (Server Components + `/api/harmonix/*` proxy for the browser).
# No NEXT_PUBLIC_* needed: client components call same-origin `/api/harmonix/...`; the proxy adds the key.
API_BASE_URL=https://your-backend.example.com
API_KEY=your-api-key-here
